[build]
  command = "npm install && npm run build"
  publish = "dist"

[build.environment]
  VITE_API_BASE = "https://stafflink-app.netlify.app/api"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
